(() => {const AKEY='emscodesim_instructor_assignments_v1',RKEY='emscodesim_debrief_reports_v1';const $=id=>document.getElementById(id);const read=k=>{try{return JSON.parse(localStorage.getItem(k)||'[]')}catch{return[]}};const write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));function code(){return Math.random().toString(36).slice(2,8).toUpperCase()}function renderAssignments(){const list=$('assignmentList'),rows=read(AKEY);list.innerHTML=rows.length?rows.map(a=>`<article class="card"><h3>${esc(a.name)}</h3><div class="meta"><span>${esc(a.scenarioLabel)}</span><span>${esc(a.group||'Open assignment')}</span>${a.due?`<span>Due ${esc(a.due)}</span>`:''}</div><p>Assignment code: <span class="code">${esc(a.code)}</span></p><div class="actions"><button data-copy="${esc(a.code)}">Copy code</button><button class="secondary" data-delete="${esc(a.id)}">Delete</button></div></article>`).join(''):'<div class="empty">No assignments created yet.</div>'}function renderReports(){const list=$('reportList'),rows=read(RKEY);list.innerHTML=rows.length?rows.map((r,i)=>`<article class="card"><h3>${esc(r.patient?.name||r.scenarioName||'Scenario report')}</h3><div class="meta"><span>${esc(r.learnerName||'Learner not entered')}</span><span>${esc(r.completedAt||r.timestamp||'Date unavailable')}</span><span>Score: ${esc(r.overallScore??r.score??'—')}%</span></div><div class="actions"><button data-report="${i}">Download report</button><button class="secondary" data-report-delete="${i}">Delete</button></div></article>`).join(''):'<div class="empty">No completed reports saved on this device.</div>'}function download(obj,name){const b=new Blob([JSON.stringify(obj,null,2)],{type:'application/json'}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=name;a.click();URL.revokeObjectURL(u)}$('createBtn').onclick=()=>{const name=$('assignmentName').value.trim();if(!name){alert('Enter an assignment name.');return}const sel=$('scenario'),rows=read(AKEY),a={id:crypto.randomUUID?.()||Date.now().toString(),code:code(),name,scenario:sel.value,scenarioLabel:sel.options[sel.selectedIndex].text,group:$('groupName').value.trim(),due:$('dueDate').value,requireDebrief:$('requireDebrief').checked,createdAt:new Date().toISOString()};rows.unshift(a);write(AKEY,rows);$('created').hidden=false;$('created').innerHTML=`Created <strong>${esc(a.name)}</strong>. Share code <span class="code">${esc(a.code)}</span>.`;renderAssignments()};$('assignmentList').onclick=e=>{const c=e.target.dataset.copy,d=e.target.dataset.delete;if(c)navigator.clipboard?.writeText(c);if(d){write(AKEY,read(AKEY).filter(a=>a.id!==d));renderAssignments()}};$('clearBtn').onclick=()=>{if(confirm('Delete all assignments?')){write(AKEY,[]);renderAssignments()}};$('refreshBtn').onclick=renderReports;$('reportList').onclick=e=>{const i=e.target.dataset.report,di=e.target.dataset.reportDelete;if(i!==undefined){const r=read(RKEY)[Number(i)];download(r,`EMSCodeSim-Debrief-${Date.now()}.json`)}if(di!==undefined){const rows=read(RKEY);rows.splice(Number(di),1);write(RKEY,rows);renderReports()}};$('importFile').onchange=async e=>{const f=e.target.files[0];if(!f)return;try{const obj=JSON.parse(await f.text()),rows=read(RKEY);rows.unshift(obj);write(RKEY,rows);renderReports()}catch{alert('That file is not valid JSON.')}e.target.value=''};renderAssignments();renderReports()})();
