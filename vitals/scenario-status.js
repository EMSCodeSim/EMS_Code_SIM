
(()=>{'use strict';
function getRecord(){try{return window.EMSCodeSimPatientRecord?.active?.()||null}catch{return null}}
function render(){const r=getRecord();let el=document.getElementById('emsScenarioStatus');if(!el){el=document.createElement('aside');el.id='emsScenarioStatus';el.hidden=true;el.setAttribute('aria-live','polite');el.innerHTML='<div class="ess-wrap"><div class="ess-main"><div class="ess-label">Active patient</div><div class="ess-title"></div><div class="ess-meta"></div></div><div class="ess-actions"><a href="/vitals/patient-record.html">View findings</a><a class="secondary ess-return" href="/vitals/scenario-launcher.html">Return to current scenario</a></div></div>';document.body.prepend(el)}if(!r){el.hidden=true;return}const findings=Object.keys(r.findings||{}).length;el.querySelector('.ess-title').textContent=[r.patient,r.title].filter(Boolean).join(' — ')||'Active EMS scenario';const back=el.querySelector('.ess-return');if(back)back.href=`/vitals/visual-patient.html?case=${encodeURIComponent(r.scenarioId||r.id)}`;el.querySelector('.ess-meta').textContent=`${findings} finding${findings===1?'':'s'} saved • patient locked across assessments${r.dispatch?' • '+r.dispatch:''}`;el.hidden=false}
function installInitialAssessmentDrill(){if(!/^\/vitals\/?$/.test(location.pathname)||document.getElementById('initialAssessmentVideoDrill'))return;const hub=document.querySelector('main.hub');if(!hub)return;const sections=Array.from(hub.querySelectorAll(':scope > section.learning-stage'));const completePatient=sections.find(section=>/Complete patient care/i.test(section.querySelector('h2')?.textContent||''));const section=document.createElement('section');section.className='learning-stage';section.id='initialAssessmentVideoDrill';section.innerHTML='<div class="stage-head"><p class="eyebrow">NREMT-style video practice</p><h2>3. Initial assessment video drill</h2><p>Watch a real-time breathing-problem clip, then identify as many scene size-up and primary-assessment findings as you can before scoring your first look.</p></div><div class="vital-grid"><article class="vital-card"><h3>Breathing Problem — Initial Assessment</h3><p>Watch the arrival video once, answer 12 scene size-up and primary-assessment questions from memory, then review what you recognized and what you missed.</p><a href="/vitals/initial-assessment-video.html">Start video drill →</a></article></div>';
if(completePatient){const heading=completePatient.querySelector('h2');if(heading)heading.textContent='4. Complete patient care';hub.insertBefore(section,completePatient);const next=sections.find(s=>/Progress and instructor tools/i.test(s.querySelector('h2')?.textContent||''));if(next?.querySelector('h2'))next.querySelector('h2').textContent='5. Progress and instructor tools';}else hub.appendChild(section)}
window.addEventListener('emscodesim:patient-record-updated',render);document.addEventListener('DOMContentLoaded',()=>{render();installInitialAssessmentDrill()});render();installInitialAssessmentDrill();
})();
